#include "shell.h"
#include "history.h"

int testmain(){
    //spliter:
    /*
    char testcmd[CMD_SIZE] = " ls -al ";
    char** tmpv;
    int tmpc;
    Spliter(testcmd, &tmpv, &tmpc);
    for(int i = 0; i < tmpc; i++){
        printf("%s\t%ld\n", tmpv[i], strlen(tmpv[i]));
    }
     */

    //recognize history
    // char his[CMD_SIZE];
    // scanf("%s", his);

    // int ans = RecognizeHisSym(his);
    // printf("%d", ans);
    
    //
    // argv[0] = "ls";
    // argv[1] = "-al";
    // argc = 2;
    // char* std_cmd = FormStdCmd();
    // printf("%s_\n", std_cmd);

    // InitHistory();

    // strcpy(cmd, "ls -al");
    // AnalyzeCmd();
    // InfoArg();
    // return 0;
}